// src/features/gamificacao/listeners/gamificacaoListener.ts
//
// STATUS: DESATIVADO — neutralizado na sessão 17 (UX Alignment Pass)
//
// Gamificação pertence ao Experience Profile Arcade — futuro.
// Código preservado intencionalmente para reativação quando Arcade existir.
// A chamada initGamificacaoListener() em dashboard/layout.tsx é mantida
// porque esta função agora é um no-op seguro — não há efeito colateral.
//
// NÃO reativar antes de:
//   - Mutation Layer implementado
//   - Experience Profiles existirem (sprint 8)
//   - Sistema Arcade ter progressão real vinculada a finanças

export function initGamificacaoListener(): void {
  // no-op — gamificação desativada até Experience Profile Arcade
}
